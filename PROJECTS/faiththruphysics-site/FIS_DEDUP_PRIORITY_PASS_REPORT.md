# FIS Dedup Priority Pass Report

Date: 2026-07-26
Agent: Codex

## Principle

FIS stays generic. No Faith Through Physics-specific logic was added to the File
Intelligence System. This pass used existing FIS primitives:

- SHA-256 exact identity grouping
- Shell-stripped normalized text extraction
- MinHash/LSH near-duplicate clustering
- Filename/article-family stem grouping
- Observe/report only; no source files were moved or deleted

Project-specific interpretation belongs in this report and later JOB outputs,
not inside the reusable FIS tool.

## Tool Used

- `D:\GitHub\David-OS\file-intelligence-system\fis_near_dup.py`
- Dependency installed: `datasketch`

## Input Scope

The full staged corpus has 46,706 copied files. A first all-at-once run against
that corpus exceeded the one-hour command window, so the successful pass used
the higher-signal source roots first:

- `E:\Site\_staging\JOB-01-html-aggregation-20260726\copies\D_GitHub_faiththruphysics-site-live`
- `E:\Site\_staging\JOB-01-html-aggregation-20260726\copies\D_GitHub_faiththruphysics-site-v2`
- `E:\Site\_staging\JOB-01-html-aggregation-20260726\copies\D_GitHub_genesis-to-quantum`
- `E:\Site\_staging\JOB-01-html-aggregation-20260726\copies\E_Faith_Through_Physics`
- `E:\Site\_staging\JOB-01-html-aggregation-20260726\copies\H_Desktop_Folders_Master_HTML`

## Output Location

`E:\Site\_staging\JOB-01-html-aggregation-20260726\fis_neardup_priority_sources`

Files:

- `exact_dups.csv`
- `near_dups.csv`
- `families.csv`
- `scan_errors.log`
- `move_plan_DRAFT.ps1`

Important: `move_plan_DRAFT.ps1` is generated by the generic scanner and is
fully commented out. Do not use it as a site action plan without separate
review; this project needs canonical selection, not tombstoning.

## Results

- Files scanned: 9,137
- Files fingerprinted: 8,668
- Scan errors: 0
- Exact duplicate groups: 882
- Near-duplicate clusters: 957
- Article/file-family groups: 1,496
- `exact_dups.csv` rows: 2,670
- `near_dups.csv` rows: 4,346
- `families.csv` rows: 4,807

## Operational Notes

- The existing FIS tools are good enough to use as the base. The full-corpus
  timeout points to a batching/configuration gap, not a need to rewrite the
  scanner for this one site.
- The generic scanner's built-in `canonical_owner` priority is stale for this
  project and should not be treated as authoritative. Use its grouping outputs,
  then apply project-level source priority in a separate report or approval GUI.
- The full JOB-01 exact hash view is already available from
  `html-inventory-clean.csv` and `duplicate-hash-summary.csv`; this FIS pass
  adds near-duplicate and family structure across the priority sources.

## Recommended Next Step

Create a generic FIS batch/profile runner that can:

- accept a target list or inventory CSV
- process large corpora in batches
- write one run ledger per batch
- keep grouping/reporting generic
- allow project-specific source priority to live in an external config/report

Then run remaining warehouse-heavy staged roots in batches and merge their
cluster IDs back into the JOB-02/JOB-10 canonical candidate map.
